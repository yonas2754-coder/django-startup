# django-startup
clone django-knowlageshare
cd django-knowlageshare
python -m venv venv
call venv\Scripts\activate
pip install django
python manage.py migrate
python manage.py runserver# django-knowlageshare
